events {
    worker_connections 1024;
}

http {
    # Rate limiting
    limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;

    upstream api_backend {
        server api:8000;
    }

    # HTTP server — serves the API and ACME challenges for certbot
    server {
        listen 80 default_server;
        server_name flywheel-dev.bioprocess.ai;

        client_max_body_size 100M;

        # ACME challenge for Let's Encrypt
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            limit_req zone=api burst=20 nodelay;

            proxy_pass http://api_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_read_timeout 300s;
            proxy_connect_timeout 10s;
            proxy_send_timeout 300s;
        }
    }

    # ─── HTTPS — uncomment after running certbot ────────────────────────
    # After obtaining certs, replace the server block above with:
    #
    # server {
    #     listen 80;
    #     server_name flywheel-dev.bioprocess.ai;
    #     location /.well-known/acme-challenge/ { root /var/www/certbot; }
    #     location / { return 301 https://$host$request_uri; }
    # }
    #
    # server {
    #     listen 443 ssl;
    #     server_name flywheel-dev.bioprocess.ai;
    #
    #     ssl_certificate     /etc/letsencrypt/live/flywheel-dev.bioprocess.ai/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/flywheel-dev.bioprocess.ai/privkey.pem;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_prefer_server_ciphers on;
    #
    #     client_max_body_size 100M;
    #
    #     location / {
    #         limit_req zone=api burst=20 nodelay;
    #         proxy_pass http://api_backend;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #         proxy_read_timeout 300s;
    #         proxy_connect_timeout 10s;
    #         proxy_send_timeout 300s;
    #     }
    # }
}
